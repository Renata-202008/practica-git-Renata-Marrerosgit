### Mi-primer-repo



Proceso Git en PowerShell

1\. `git config --global user.name "Ali"`

2\. `git config --global user.email "ali@gmail.com"`

3\. `mkdir MiProyecto; cd MiProyecto`

4\. `ni notas.txt` + `echo "hola" > notas.txt`

5\. `git init`

6\. `git add -A`

7\. `git commit -m "Primer commit"`

